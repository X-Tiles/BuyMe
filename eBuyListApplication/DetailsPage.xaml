<phone:PhoneApplicationPage
    x:Class="eBuyListApplication.DetailsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:toolkit="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls.Toolkit"
    d:DataContext="{d:DesignData SampleData/MainViewModelSampleData.xaml}"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait"  Orientation="Portrait"
    shell:SystemTray.IsVisible="True">

    <!--Data context is set to sample data above and first item in sample data collection below and LayoutRoot contains the root grid where all other page content is placed-->
    <Grid x:Name="LayoutRoot" Background="Transparent" d:DataContext="{Binding Items[0]}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!--TitlePanel contains the name of the application and page title-->
        <StackPanel x:Name="TitlePanel" Grid.Row="0" Margin="12,17,0,28">
            <TextBlock Text="BUYME" Style="{StaticResource PhoneTextNormalStyle}"/>
            <TextBlock Text="{Binding Name}" Margin="9,-7,0,0" Style="{StaticResource PhoneTextTitle1Style}"/>
        </StackPanel>
        <!--Ukryte - dodaj produkt textbox-->
        <StackPanel Grid.Row="1" 
                    x:Name="GridForAddListTextBox" 
                    Margin="0,0,0,0">
            <TextBox x:Name="AddNewProductTextBox" 
                     Visibility="Collapsed" 
                     TextChanged="AddNewProductTextBox_OnTextChanged">
            </TextBox>
        </StackPanel>
        <StackPanel Grid.Row="2" x:Name="GridForComboBoxSearch">
            <ComboBox x:Name="SearchProductComboBox" 
                      Visibility="Collapsed" 
                      Background="White"
                      ItemsSource="{Binding}"
                      FontSize="30"
                      Padding="5"
                      Margin="30,0,0,50"
                      Tap="SearchProductComboBox_OnTap"
                      SelectionChanged="SearchProductComboBox_OnSelectionChanged"
                      >
            </ComboBox>

        </StackPanel>
        <!--Ukryte - dodaj produkt button-->
        <StackPanel Grid.Row="3" 
              x:Name="GridForAddListButton" 
              Margin="0,-20,0,0">
            <Button x:Name="AddNewProductButton" Visibility="Collapsed" Click="AddNewProductButton_Click"></Button>
        </StackPanel>

        <!--ContentPanel contains details text. Place additional content here-->
        <Grid x:Name="ContentPanel" Grid.Row="4" Margin="12,0,12,0">
            <Button Width="200" Height="100"></Button>
            <phone:LongListSelector x:Name="DetailsLongListSelector" 
                                    Margin="0,0,-12,0" 
                                    Background="Black" 
                                    ItemsSource="{Binding}">
                <phone:LongListSelector.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="0,0,0,17">
                            <toolkit:ContextMenuService.ContextMenu>
                                <toolkit:ContextMenu Background="White" IsZoomEnabled="True" Name="ProductsContextMenu">
                                    <toolkit:MenuItem x:Name="Edit" Foreground="Black" Header="Edytuj" Click="ProductEdit_OnClick"></toolkit:MenuItem>
                                    <toolkit:MenuItem x:Name="Delete" Foreground="Black" Header="Usuń" Click="ProductDelete_OnClick" ></toolkit:MenuItem>
                                </toolkit:ContextMenu>
                            </toolkit:ContextMenuService.ContextMenu>
                                <TextBlock x:Name="ProductTextBlock"
                                      Text="{Binding Name}"
                                      Style="{StaticResource PhoneTextExtraLargeStyle}"
                                      Tap="ProductTextBlock_OnTap">
                                </TextBlock>
                            <toolkit:ToggleSwitch x:Name="IsBoughtToogle" IsChecked="{Binding IsBought}" SwitchForeground="Green" Checked="IsBoughtToogle_OnChecked" Loaded="IsBoughtToogle_OnLoaded" Unchecked="IsBoughtToogle_OnUnchecked"  ></toolkit:ToggleSwitch>
                        </StackPanel>
                    </DataTemplate>
                </phone:LongListSelector.ItemTemplate>
            </phone:LongListSelector>
        </Grid>

        <!--Uncomment to see an alignment grid to help ensure your controls are
            aligned on common boundaries.  The image has a top margin of -32px to
            account for the System Tray. Set this to 0 (or remove the margin altogether)
            if the System Tray is hidden.

            Before shipping remove this XAML and the image itself.-->
        <!--<Image Source="/Assets/AlignmentGrid.png" VerticalAlignment="Top" Height="800" Width="480" Margin="0,-32,0,0" Grid.Row="0" Grid.RowSpan="2" IsHitTestVisible="False" />-->
    </Grid>

    <phone:PhoneApplicationPage.ApplicationBar>
        <shell:ApplicationBar IsVisible="True" IsMenuEnabled="True">
            <shell:ApplicationBarIconButton IconUri="Assets/appbar.add.rest.png" 
                                            Text="dodaj" 
                                            Click="AddProductApplicationBarIconButton_OnClick"/>
            <!--<shell:ApplicationBar.MenuItems>
                <shell:ApplicationBarMenuItem Text="MenuItem 1"/>
                <shell:ApplicationBarMenuItem Text="MenuItem 2"/>
            </shell:ApplicationBar.MenuItems>-->
        </shell:ApplicationBar>
    </phone:PhoneApplicationPage.ApplicationBar>

</phone:PhoneApplicationPage>